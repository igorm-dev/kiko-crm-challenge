# Workspace packages always resolve to the local source, never the registry.
link-workspace-packages=true
prefer-workspace-packages=true

# Fail fast if the declared packageManager version does not match.
engine-strict=true
